---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

type Props = {
  src: string;
  alt: string;
  width: number;
  height: number;
  metadata: ImageMetadata;
};
---

<!-- 画像の設定 -->
<figure>
  <Image {...Astro.props} class="test" />
  <figcaption>
    <slot />
  </figcaption>
</figure>

